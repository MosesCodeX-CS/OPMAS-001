[Unit]
Description=OPMAS Modbus PLC Simulator Service
After=network.target

[Service]
Type=simple
User={{USER}}
WorkingDirectory={{WORKING_DIR}}
ExecStart={{PYTHON_PATH}} {{WORKING_DIR}}/simulator.py
Restart=on-failure

[Install]
WantedBy=multi-user.target
